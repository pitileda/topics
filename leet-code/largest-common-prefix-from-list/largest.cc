#include <iostream>
#include <vector>
#include <map>
#include <string>
#include <list>

using namespace std;

class Solution{
private:
  map<string, vector<uint32_t>> matches;
  list<vector<uint32_t>> ranges
public:
  
  string largestCommonPrefix(vector<string>& strs){

  }
}

int main(){
  return 0;
}